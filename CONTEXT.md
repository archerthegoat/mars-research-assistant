# Mars Skills 词汇表

**Mars Research Assistant 组合 Skill**：对外安装和调用的完整研究助手，统一承载
Ask Mars、市场催化剂简报、市场快照、个股快览、深度研究、技术面分析、投研分析与 Drive
写入八项子 Skill。
_Avoid_：Mars Skills 集合、八个独立安装包

**RED Skill 分发入口**：组合包根 `SKILL.md` 所代表的上传与安装入口。完整包可用时它负责
编排八个子 Skill；上传平台只保留 Markdown 时，它只能披露权限并引导 npx 安装，不能宣称
依赖脚本已经可运行。
_Avoid_：第八个研究 Skill、Matt 风格根 Skill

**Skill 运行包**：面向终端用户安装的 Mars Research Assistant 最小完整文件集，包含根
入口、八个子 Skill、运行期脚本、锁文件和必要的内置资源；不包含测试、开发文档、源码仓库
元数据或人类阅读的 README。它位于开发仓库的
`skills/mars-research-assistant/`，而不是开发仓库根目录。
_Avoid_：完整仓库、开发副本、发布 ZIP

**公开安装入口**：通过 `npx skills add archerthegoat/mars-research-assistant --skill
mars-research-assistant --agent codex --global --copy` 安装 Skill 运行包的唯一用户路径。
安装只复制运行包；技术面分析首次被调用时才准备其专属依赖环境。
_Avoid_：curl 一键安装、受管安装器、额外 npm CLI

**子 Skill**：Mars Research Assistant 组合 Skill 内具有独立触发条件和交付边界的一项能力；
共享同一个顶层安装包，可以被直接调用，但不会因此混合不同研究任务。
_Avoid_：独立安装 Skill、插件

**Ask Mars**：只根据当前情况建议应使用哪些 Mars Skill 及其顺序、当前第一步
及所需最少输入；不自动执行研究、取数、生成研究交付或写入 Drive。

**独立研究 Skill**：只完成一种研究交付，不在内部负责其他研究类型的路由。

**市场催化剂简报**：面向目标市场的近期催化剂研究，涵盖宏观/政策、重要
财报、地缘政治、贸易与监管事件，以及正在发展的重大风险。

**市场快照**：以 Markdown 交付当前市场状态，优先覆盖利率与通胀、风险偏好与
信用、美元与大宗商品三组核心指标；数据不足时显示缺口，而非强行补全。

**个股快览**：在核验单一明确标的的发行人身份后，交付一组带 `as_of` 的关键公开数据
（价格、规模或估值锚、最新财务摘要）、最近 30 天内 3–5 条公司直接相关的公告或新闻，
以及来源、时间戳和明确的数据缺口；不包含估值、技术面、宏观判断或交易建议。
_Avoid_：标的研究、深度研报、行情快讯

**发行人身份核验**：个股研究在取数和创建研究工件前，对公司名称、代码、交易所和实际
发行人的一致性进行的检查。无法唯一对应时必须要求用户澄清，不能猜测对象或生成可能归属
错误的工件。
_Avoid_：同名公司猜测、代码与发行人不一致、先报告后澄清

**个股研究机器标识**：为兼容已有调用，个股快览保留 `instrument-research` 作为目录名和
调用标识；深度研究使用 `deep-equity-research`。面向用户的名称分别为“个股快览”和
“深度研究”；v1.0.3 另增独立的“投研分析”（`investment-analysis`），不得与深度研究混用。
_Avoid_：破坏性重命名、以目录名替代用户名称、复用同一个标识表示两种深度

**深度研究**：面向单一明确标的的完整基本面承保研究。v1.0.2 基线是九章研报、来源层级、
质量核查和三情景 DCF/反向 DCF；v1.0.3 已实现多市场、财报 A–D、可复算扩展估值、
技术证据交集和仅多头、持仓无关的条件式交易方案。默认研究/持有期为 1–6 个月波段，
不做空、不访问账户/券商/订单。
_Avoid_：个股快览、卖方荐股、自动交易策略、账户级仓位建议

**九章研报骨架**：深度个股研究的固定交付顺序：研究范围与核心判断；公司与商业模式；
行业与竞争格局；管理层、治理与资本配置；财务表现与质量核查；预期差、催化剂与关键
跟踪项；三情景 DCF/反向 DCF（仅数据齐备时）；风险、反方论点与可证伪条件；来源、
时间戳、假设与数据缺口。
_Avoid_：按标的任意增删核心章节、没有输入仍展示估值、不可追溯的结论

**财报质量核查**：v1.0.2 的最小检查覆盖现金转换、应计、收入/利润/经营现金流一致性、
稀释/SBC、审计与治理红旗。v1.0.3 在字段和会计可比时增加 Beneish M-Score、收入确认红旗，
并以可追溯规则输出 A–D；C/D 直接否决多头入场方案，但不自动产生做空建议。
_Avoid_：无证据评级、伪精确 A–D、缺数据时静默补全

**个股研究路由**：根据用户明确意图在个股快览和深度个股研究之间选择交付的规则。未指定
深度的单标的请求默认进入个股快览；只有明确的深度研究、估值、财报复盘、投资备忘录、
竞争格局或建模意图才进入深度个股研究；Ask Mars 对不清晰请求优先推荐个股快览并说明
可升级路径。
_Avoid_：隐式重型流程、按模型猜测深度、一次请求两种报告

**估值假设集**：由 `scripts/dcf.py` 消费的可追溯 JSON 输入。v1.0.3 支持 DCF、反向 DCF/
PVGO、概率加权情景、EPV/三要素、EVA/剩余收益、适用时 SOTP 和可选蒙特卡洛；不适用或
缺输入必须显式记录，不能心算、伪造或静默替换模型。
_Avoid_：模型猜测的估值输入、心算估值、无来源目标价

**技术面分析**：面向价格结构、关键位、情景与失效条件的技术研究；不自动
包含基本面或行业叙事，也不绑定特定作者的专属方法论。
_Avoid_：旧能力名称、人物品牌化命名、技术分析报告

**技术面分析工件包**：一次技术面分析的持久交付，由承载理由分析的 `analysis.md` 和用于
审计重放的 `evidence.json` 组成；两者共享同一个 evidence_id。每次合格调用另生成临时
`chart.html` 作为交互视图，但它不属于持久工件包。
_Avoid_：技术面分析报告、单文件报告

**本地研究工件**：一次最终研究交付在活动工作目录中创建的用户可见文件。文字交付必须
包含 Markdown 源文件；PDF 或 PowerPoint 只在用户要求或能显著改善图表、比较或叙事时
额外生成。它不授权隐式缓存、历史记录或 Drive 归档。
_Avoid_：仅聊天正文、隐藏持久状态、自动云端写入

**研究工作目录**：活动工作目录下默认名为 `mars-research/` 的本地研究工件目录。用户
可以显式指定其他目录；每次交付使用唯一文件名且绝不覆盖已有工件，运行包安装目录不属于
研究工作目录。
_Avoid_：Skill 安装目录、隐藏缓存目录、自动 Drive 归档

**Skill 性能预算**：对公开运行包与确定性本地任务设定的验收边界：运行包不超过
1.5 MiB 和 80 个文件（v1.0.3 重定基线；v1.0.2 为 1 MiB / 60 个文件），个股快览与
深度研究的离线 fixture Markdown 生成各不超过 1 秒。
安装流程移除 uv/yfinance 预构建，并在同一机器上记录相对旧 curl 流程至少 50% 的端到端
耗时缩短；网络克隆耗时只报告，不作为 CI 硬门槛。
_Avoid_：只测开发测试套件、未分离网络的安装时限、无基线的性能宣称

**技术面证据集**：一次分析中唯一的标准化 OHLCV、来源信息和确定性派生结果集合。
技术面图表和理由分析只能引用该集合；理由分析可以解释证据，但不能创造新的数值证据。
它以 `evidence.json` 随工件包交付。
_Avoid_：图表数据、分析上下文、模型推断数据

**市场背景上下文**：由前置市场快照提供、带独立来源和 as_of 的可选背景。技术面分析可以
说明它与技术证据的共振或冲突，但不能让它改变图表、指标或关键位；缺失时必须明确声明
本次情景仅基于技术面证据。它与技术面取数并行且非阻塞，获取失败不能阻止技术面分析交付。
标准市场背景在目标市场时区内默认 24 小时有效。
_Avoid_：宏观补全、隐含市场背景

**可追溯关键位**：由确认摆动点的确定性聚类和排序产生的支撑或阻力；每个价位都保留
method、lookback、anchor_dates、touches 和 price。某一侧没有合格摆动位时，可以使用
明确标记的 120 日最高或最低价。
_Avoid_：模型关键位、手动画线

**技术面图表**：每次合格技术证据调用生成的临时单文件 HTML；使用固定版本的
Lightweight Charts，展示同一份已审计 OHLCV、成交量、均线、ATR 与可追溯关键位，并标记
symbol、source、timezone、as_of、adjustment 和 bars_used。v1.0.3 的承保注释图只能在技术质量门
通过且价值/技术区间有交集时生成，不能在图上创造新的价位。
_Avoid_：静态 SVG、持久图表工件、在线行情图

**免费基础能力**：不要求用户提供 API key 也能完成的公开/官方来源研究；
数据不足时如实呈现缺口。

**内置 OHLCV 数据源**：v1.0.2 技术面基线使用零配置 yfinance best-effort；v1.0.3 深度研究
已实现显式数据源适配器，覆盖美股、港股、A 股。provider 切换必须记入来源账本，不能静默
fallback；不支持或过期时明确失败。
_Avoid_：无来源行情、静默回退、把单一 provider 当作所有市场承诺

**公开研究来源**：市场快照、市场催化剂简报和标的研究为核验事实而访问的政府、监管机构、
交易所、发行人及其他无需私密凭据的公开来源。
_Avoid_：私密行情供应商、账户数据

**研究来源层级**：核验个股快览或深度个股研究事实时使用的公开来源优先顺序：监管披露、
交易所和发行人 IR 优先；价格、规模和估值锚使用可公开核验的行情来源并标明 `as_of`；
公司公告优先于可信媒体原文。搜索摘要不是证据，冲突或缺失必须明确披露。
_Avoid_：无来源的市场数据、搜索摘要证据、私有 API 数据

**同源补取**：技术面分析首次取数不合格时，对 yfinance 执行的一次扩大历史窗口重试。
它可以剔除未完成日线，但不能插值、合成数据或切换来源；仍不合格时必须报告精确缺口。
_Avoid_：自动回退、数据修复

**自定义 OHLCV 数据源**：用户在自己的副本中自行增加或替换的数据来源；不属于组合 Skill
承诺兼容、测试或支持的公共能力。
_Avoid_：备用数据源、回退数据源

**Drive 投研工作台**：用户 Google Drive 中跨设备记录交易想法、当前交易方案、证据/决策变更
和复盘的工作台。一个标的/投资案例一个主文档，深度研报、估值 JSON、技术图表保留本地并
通过 artifact ID 或链接引用；它不是 Markdown 备份库。
_Avoid_：研究文件夹、自动云端归档、后台镜像

**Drive 写入**：深度研究或投研分析本地先生成提议，用户明确确认后才创建/更新工作台主文档。
写入使用稳定 `plan_id` 和递增 `revision`；父版本冲突必须显式回报。Drive 不可用时保存可见
本地 outbox，重试前复核父 revision，不后台上传。
_Avoid_：首次调用自动写入、静默覆盖、把完整研究 JSON 倾倒到 Drive

**Drive 写入 Skill**：负责展示拟写入目标、生成提议、在明确确认后写入并读回核验；它不研究、
不替用户决定方案，也不代替用户确认。v1.0.3 的工作台主文档四区为想法日志、当前方案、
证据/决策变更、复盘。

**投研分析**：独立的 `investment-analysis` skill。输入必须是用户明确指定的本地 artifact、
Drive 文档链接/ID 或 `case_id`；默认只做增量讨论卡和方案 revision，不广泛搜索、不重跑深度研究、
不自动调用其他 skill。输出 Markdown + JSON，写 Drive 仍需确认。

**交易方案**：持仓无关的多头条件式方案，默认 1–6 个月波段；包含 `entry_plan`、`target_plan`、
`invalidation_plan` 或 `watch`，以及方案级下行幅度、收益/风险比和风险触发器。不包含账户、
仓位比例、做空或订单动作。

**研究 artifact 身份**：`issuer_id` 表示发行人，`listing_id` 表示交易所/代码/币种上市身份，
`case_id` 表示投资案例；A/H 对比共用一个 `case_id` 挂载两个 listing。artifact 之间通过这些
字段和版本信息连接，不用裸 ticker 作为唯一主键。

**市场范围偏好**：安装器支持时安装后询问；否则首次调用强制多选美股、港股、A 股、A/H 对比。
偏好写入每台设备工作区的可见文件，包含 `enabled_market_scopes` 和可选 `default_market_scope`。
明确交易所优先，裸代码多匹配时询问；市场偏好不会隐式跟随 Drive。

**行业规则包**：v1.0.3 的十六类行业以可配置注册表交付，每类必须改变关键 KPI、历史重建字段、
预测驱动、估值方法、反证框架和最低数据要求；研究运行时按目标行业加载，不把十六类全文展开。
